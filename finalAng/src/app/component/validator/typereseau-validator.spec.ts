import { TypereseauValidator } from './typereseau-validator';

describe('TypereseauValidator', () => {
  it('should create an instance', () => {
    expect(new TypereseauValidator()).toBeTruthy();
  });
});
